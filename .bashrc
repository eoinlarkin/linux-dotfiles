#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# Setting neofetch to run on start
# neofetch

# Purple color prompt
export PS1="\e[0;35m[\u@\h \W]\$ \e[m "
